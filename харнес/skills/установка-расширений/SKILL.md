---
name: установка-расширений
description: Когда наступило условие из таблицы кандидатов (первый фронт, первый запрос документа, первая внешняя библиотека, закрытая приёмка, пропуски скилов в журнале) или в задаче появилось мобильное/клиентское приложение — установить расширение или крупный набор («возможность») по процедуре
---

> Сгенерировано из глав(ы) пакет скриптом SBORKA-SKILOV.py —
> НЕ ПРАВИТЬ РУКАМИ: правка вносится в главу, затем перегенерация.
> Рабочие каталоги задач: .tasks/<id>/ в корне проекта (создаются на задачу).

<!-- дословно из skills-src/установка-расширений.md (файл пакета) -->
## Порядок установки любого расширения

1. Сверить условие срабатывания с таблицей ниже — ставится только расширение,
   чьё условие НАСТУПИЛО. Ставить по одному.
2. Анонс владельцу в канал: что ставлю, по какому условию, что даст.
3. Выполнить команды установки из таблицы. После любых `/plugin ...` —
   обязательно `/reload-plugins`; проверить статус в `/plugin`.
4. Записать: цель установки + дата → запись в `память/` (type: reference);
   строка в реестр механизмов (`$LOG_DIR/evo/реестр-механизмов.yaml`).
5. Через месяц — перемер по журналу вызовов: ноль вызовов → разбор по
   процедуре «ноль вызовов — диагноз» (UNIFIED/13), снос — с формулировкой
   и проверкой диска фактом.

## Таблица кандидатов: условие → команды

| условие (наблюдаемое) | расширение | команды |
|---|---|---|
| первая задача с UI/фронтом | playwright + frontend-design | `/plugin install playwright@claude-plugins-official` · `/plugin install frontend-design@claude-plugins-official` · `/reload-plugins` |
| владелец впервые просит документ (docx/xlsx/pptx/pdf) | example-skills | `/plugin marketplace add anthropics/skills` · `/plugin install example-skills@anthropic-agent-skills` · `/reload-plugins` |
| в проекте появилась первая внешняя библиотека | Context7 MCP | bash: `claude mcp add context7 npx @context7/mcp` · проверка: `claude mcp list` |
| чек-лист приёмки закрыт И владелец подтвердил | claude-mem | `/plugin marketplace add thedotmack/claude-mem` · `/plugin install claude-mem` · `/reload-plugins` · задать потолок памяти службы в harness.conf · включить cron `fix-claude-mem-project.py` |
| у продукта появился веб-сервер (первый выкат с HTTP) | эндпоинт dev-map для дашборда-«пульта харнеса» | задача через полный пайплайн (не наспех): `GET /api/v1/dev-map/data` → JSON `{project: {id, name}, snapshot: {generated_at, tasks[], milestones[]}}` из dev-map.yaml; CORS разрешён для хаба; кэш ~5 мин; снимок с признаком устаревания (>2 суток — карта сама говорит «устарело»); кривой манифест НЕ затирает последний хороший снимок; ~30 строк на стеке продукта. После выката сообщить владельцу URL — он добавит сервер строкой в projects.json своего хаба |
| владелец впервые прислал голосовое | контур голосовых | bash: `pip install openai --break-system-packages` · ключ в `$SECRETS_DIR/openai_api_key` · достроить скачивание (getFile) в диспетчере как прибор по 08-НОРМЕ (доказать на своём голосовом) · проверка: `rasshifrovat_golos.py` на эталонной записи |
| журнал skills.jsonl за месяц: обязательный скил пропущен ≥3 раз | toolkit-skills | `/plugin marketplace add spences10/claude-code-toolkit` · `/plugin install toolkit-skills@claude-code-toolkit` · `/reload-plugins` · перемер доли пропусков через месяц |

Браузер ставится ОДНИМ механизмом (плагин ИЛИ MCP `claude mcp add playwright
npx @playwright/mcp`) — не оба.

## Не ставить (вердикты 09.08.2026; пересмотр — только по записи в журнале дефектов)

| расширение | причина одной строкой |
|---|---|
| feature-dev, superpowers | второй пайплайн рядом с нашим — конфликт с гейтами и скилами пакета |
| devops-automation-pack | предлагает выкат мимо `deploy.sh` — конфликт с инвариантом И-2 |
| engineering-workflow-skills | ниши закрыты: `падение-тестов`, формат коммита в гейте |
| code-operations-skills | ниша закрыта записью [[feedback-data-via-scripts]] |
| ui-ux-pro-max | подмножество frontend-design; два дизайн-гайда конфликтуют |
| dream-skill / Auto Dream | фоновая правка памяти без подтверждения — против нормы ревизии (UNIFIED/03) |
| n8n-MCP | нет потребителя; пересмотр при первой интеграции продукта |

## Возможности: крупные наборы ставятся не строкой таблицы, а одной командой

Строка таблицы выше — это одна-две команды. Набор, которому нужны системные
пакеты, скрипты в `~/bin`, свои скилы, свои запреты сторожу и свой реестр
действий владельца, строкой не описывается: он живёт каталогом с паспортом в
`харнес/возможности/<id>/` и ставится менеджером:

```bash
bash scripts/vozmozhnost.sh список                  # что есть и при каком условии ставится
bash scripts/vozmozhnost.sh требования <id>         # замер ДО установки, с адресом у каждого «нет»
bash scripts/vozmozhnost.sh поставить  <id>         # установка и врастание в носители
bash scripts/vozmozhnost.sh состояние               # что стоит и не менялся ли паспорт
```

| условие (наблюдаемое) | возможность | команда |
|---|---|---|
| в задаче продукта появилось мобильное или клиентское приложение (React Native, Expo, Android, iOS) | `мобильная-разработка` — эмулятор как постоянный ресурс, петля сборка→установка→сценарий→скриншот, реальные устройства Test Lab с потолком расхода, сборка iOS без Mac | `bash scripts/vozmozhnost.sh поставить мобильная-разработка` |

Что делает установка сама, помимо самих инструментов: собирает скилы набора
(до установки их НЕТ — контекст чист), добавляет его запреты сторожу и каталоги
демону уборки, кладёт запись в память со строками указателя и таблицы
срабатываний, подключает его гейты к pre-commit и кладёт реестр действий
владельца в `docs/возможности/<id>-РУКАМИ.md`.

**Реестр действий владельца отправить ему ВЛОЖЕНИЕМ** сразу после установки:
там браузерные шаги, оплаты и проверки, которые физически не сделать из сессии.
Приёмка набора — его собственный `ЧЕК-ЛИСТ.md`; до её прохождения работа с
набором носит статус `test`, не `done`.

Снятие: `vozmozhnost.sh снять <id>` — скилы исчезают, запреты и уборка
перестают действовать; что осталось на диске и как это снести, перечислено в
разделе «Снятие» чек-листа набора (сносит владелец, не сессия).

## Дополнение таблицы

Новый кандидат-расширение добавляется строкой в ЭТОТ файл
(`харнес/skills-src/установка-расширений.md`) с наблюдаемым условием и
командами, затем `python3 харнес/SBORKA-SKILOV.py` — скил пересобирается.
Кандидат без условия срабатывания не добавляется.

Новая ВОЗМОЖНОСТЬ добавляется каталогом `харнес/возможности/<id>/` с
`МАНИФЕСТ.conf` (паспорт: условие, требования, пороги) и `ustanovit.sh`
(шаги, `--dry-run`, `--selftest`); строка в таблицу выше — тем же порядком.
Устройство каталога и что в нём необязательно — в шапке
`харнес/scripts/vozmozhnost.sh`.
