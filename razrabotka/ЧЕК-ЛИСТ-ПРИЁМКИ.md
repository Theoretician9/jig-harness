# ЧЕК-ЛИСТ ПРИЁМКИ: как убедиться, что харнес встал

> Проходят двое: агент выполняет и прикладывает вывод команд, владелец
> подтверждает пункты со «: владелец». Пункт без команды/факта не считается
> пройденным. Порядок — сверху вниз.

## А. Установка (USTANOVIT.sh = шаги 0–9 из 01-SPEC)

- [ ] `bash USTANOVIT.sh --selftest` — «зелёный (16 путей)»: разбор ответов-путей доказан, включая улику 11.08 (склеенная вставка `СТАРТОВ/var/www/...` → отказ).
- [ ] **Больной случай живой установки:** вписать в `install.conf` относительный `PROJECT_DIR` и запустить `--resume` → СТОП «ни один шаг не начинался» с названной причиной; вернуть верный путь → проходит.
- [ ] Оба юнита выверены systemd ДО включения: в выводе есть `systemd-analyze verify /etc/systemd/system/harness-agent.service` и то же для `harness-dispatcher` — без замечаний.
- [ ] `sudo bash USTANOVIT.sh --dry-run` прогнан до живого запуска; живой запуск завершился «Упавших шагов нет» (или упавшие починены и `--resume` дошёл до конца).

- [ ] `bash -n /etc/harness/install.conf && source /etc/harness/install.conf` — конфиг читается; PROJECT_NAME/PROJECT_DIR/SECRETS_DIR/TG_CHAT_ID заполнены.
- [ ] `claude --version` = `CC_VERSION` из конфига; headless-вызов отвечает.
- [x] `systemctl is-active harness-agent` → active — и повторно после тестового ребута. **11.08, ребут 23:43:35:** при `uptime` 0 мин → `active`; `MainPID` 967 = `tmux … claude --dangerously-skip-permissions`, потомок 974 — живой `claude`.
- [ ] После раскладки: `systemctl is-active harness-dispatcher` → active (вариант Б — обязательный контур: ack, inbox, подтверждения); второй экземпляр вручную отказывается стартовать.
- [ ] `stat -c '%a %U' "$SECRETS_DIR"` → `700 agent`; токен бота — 600.
- [ ] Проба записи файла headless-вызовом прошла без запроса разрешений (01-SPEC §6).

## Б. Канал — факт, не лог

- [ ] Отправка: `SENT_ID` непуст (`ok:true` + `message_id`).
- [ ] **: владелец** — сообщение реально пришло в Telegram.
- [ ] Обратное направление через диспетчер: владелец отвечает «дошло» → файл появляется в `$LOG_DIR/inbox/` (`ls -t | head -1`, `cat` показывает текст). Ручной `getUpdates` НЕ использовать — украдёт апдейт у диспетчера и вгонит его в 409-паузу.
- [ ] Ack ≤30 с: владелец пишет боту произвольное сообщение — подтверждение приходит в полминуты.
- [ ] Больной случай: временный конфиг с битым токеном — `HARNESS_INSTALL_CONF=/tmp/битый.conf scripts/tg_send.sh test` (в конфиге TG_TOKEN_FILE указывает на файл со строкой `битый`) → ненулевой код + строка в `tg_failures.log`; настоящему владельцу ничего не уходит.
- [ ] `emergency-send.sh` доходит до владельца мимо харнеса.

## В. Раскладка и гейты

- [ ] `razlozhit-harnes.sh` прогнан; `git init` сделан; pre-commit подключён симлинком (`ls -l .git/hooks/pre-commit`).
- [ ] `python3 scripts/hooks/test_guard_bash.py` — все пути зелёные, 0 неудач (в т.ч. обходы: env-префиксы, обёртки, docker-compose v1, пути к бинарям).
- [ ] Живой больной случай И-2: в сессии агента `docker compose restart x` → отменено сторожем с объяснением.
- [ ] Живой больной случай И-3: файл `token = "test123"` в репо → `check-secrets.sh` красный; после удаления зелёный.  <!-- не-секрет: это образец больного случая -->
- [ ] Гигиена контекста: раздутый STATE.md (>10k во временной копии) → `check-context-hygiene.py` красный.
- [ ] Кривой dev-map (сломанное поле во временной копии) → валидатор в pre-commit красный.
- [ ] Раздутый dev-map (копия >30k знаков) → `check-context-hygiene.py` красный с рецептом архивации.
- [ ] Битый JSON на входе сторожа → команда пропущена И строка в `guard_exceptions.log`; прирост лога назван в следующей сводке heartbeat.
- [ ] Журнал гейтов пишется: `tail $LOG_DIR/gates.jsonl` после пробного коммита.
- [ ] **Сквозной прогон возможностей** (путь целиком, не детали): `bash SKVOZNOJ-PROGON-VOZMOZhNOSTEJ.sh` — «зелёный, неудач 0». Именно он нашёл недоставку `skills-src/`, которую пропустили все селфтесты.
- [ ] Возможности доставлены и НЕ установлены: `bash scripts/vozmozhnost.sh список` показывает `[ не стоит  ] мобильная-разработка` с условием включения.
- [ ] Больной случай механизма возможностей: `bash scripts/vozmozhnost.sh --selftest` зелёный (7 проверок, в т.ч. «непроходимое требование → установщик не запускался»).
- [ ] Установщик набора исправен: `bash harness/vozmozhnosti/mobilnaya-razrabotka/ustanovit.sh --selftest` зелёный (6 проверок).
- [ ] Выключенный набор ничего не меняет: `ls harness/skills/` НЕ содержит `mobile-*`; `test_guard_bash.py` содержит путь «набор не установлен → его запреты молчат» и он зелёный.
- [ ] Повторная раскладка не отключает установленный набор: `razlozhit-harnes.sh --selftest` зелёный (проверка метки `.установлено` внутри).

## Г. Демоны и heartbeat (01-SPEC, шаги 8–9)

- [ ] `bash harness/demons/session-warden.sh --selftest` — «зелёный (11 путей решения по панели)».
- [ ] `bash harness/demons/sentinel.sh --selftest` — «зелёный (4 пути по метке + 4 по юниту)».
- [ ] **Больной случай «cron не бежит»:** состарить метку (`touch -d '40 minutes ago' /var/lib/harness/heartbeat/session-warden`) → `systemctl start harness-sentinel` пишет «cron не запускает демонов», запускает сторожа сессий сам, владельцу уходит сообщение с перечнем сделанного.
- [x] **Тестовый ребут (обязателен):** `sudo reboot` → после загрузки `systemctl is-active harness-agent` = active, в панели `claude`, и владельцу пришёл отчёт `harness-sentinel-boot` со строками по каждой системе. **11.08:** ребут 23:43:35 → «агент поднялся за 0с (в панели: claude)» 23:43:36, пинок 23:43:42, отчёт 23:43:55, юнит `status=0/SUCCESS`. В отчёте красных строк нет (юниты active, сторож команд 122/0, секреты чисто, сторож сессий зелёный). Приход отчёта в Telegram — **: владелец**, `tg_send` вернул успех, новых записей в `tg_failures.log` нет.
- [x] **Больной случай «tmux слетел целиком»:** `tmux kill-server` → юнит (`Restart=always`) поднимает сессию с агентом за ≤30 с; в `journalctl -u harness-agent` виден перезапуск. **12.08:** проведено на юните-двойнике (тот же `Type=forking`+`Restart=always`, свой сокет `tmux -L проба` — боевая сессия не обрывалась): kill-server 23:53:10 → сессия вернулась 23:53:20, **10 с**, `MainPID` 4871 → 4932. Проба нашла три дефекта, все починены (см. `ИЗМЕНЕНИЯ.md` за 12.08). На боевой сессии случай не воспроизводился: доказан механизм юнита, а не поведение конкретно сессии `agent`.
- [ ] **Больной случай «агент выпал в оболочку»:** в сессии агента выйти из Claude Code (`/exit`), отключиться (`Ctrl+B D`) и НЕ подключаться. В течение `AGENT_DEAD_MIN`+10 мин: в панели снова `claude` (`tmux display-message -p -t agent '#{pane_current_command}'`), в Telegram пришло «Агент выпадал в оболочку», в `rotation.jsonl` есть `agent_revived`.
- [ ] **Обратный случай (сторож не мешает):** подключиться к сессии и сидеть в оболочке 20 минут — сторож НЕ шлёт команд в панель, в логе строка «к сессии подключён владелец — не вмешиваюсь».

- [ ] `crontab -l -u agent` совпадает с таблицей §9.
- [ ] Каждый демон прогнан `env -i HOME=... PATH=... /bin/sh -c "..."` → exit 0 (кроме честных отказов, названных в логе).
- [ ] Селфтесты: `task-closer.sh --selftest`, `heartbeat-watch.sh --selftest`, `tokens-collector.sh --selftest`, `check-secrets.sh --selftest`, `handover-check.sh --selftest`, `razlozhit-harnes.sh --selftest`, `pre-commit-hook.sh --selftest-devmap` — все зелёные.
- [ ] После первых суток: `ls -l /var/lib/harness/heartbeat/` — метки свежее периодов.
- [ ] **Больной случай сторожа сторожей**: закомментировать один из частых демонов (session-warden / task-closer / evo-collector — порог = период×2, суточные всплывут лишь через двое суток) → завтрашняя сводка называет его по имени; **: владелец** — сводка приходит КАЖДОЕ утро, и зелёная тоже (её отсутствие = авария).
- [ ] Бэкап: `backup.sh` прогнан, восстановление `git clone` из bundle + распаковка tar на чистый каталог — файлы читаются.
- [ ] Пороги `harness.conf` сверены с фактическим тарифом VPS (CPU/RAM/диск → 00-ВВОДНЫЕ §2 дозаполнен).

## Д. Ротация сессий — бесшовность доказана переходом

- [ ] `pre_compact.sh`: тестовое событие пишется в `rotation.jsonl`.
- [ ] Принудительная первая ротация (03-ЗАДАНИЕ §3): анонс → подтверждение владельца → штатное завершение → новая сессия **называет текущую задачу и следующий шаг без подсказок**.
- [ ] Результат в `rotation.jsonl`; потери контекста разобраны правкой шаблона передачи (если были).
- [ ] `CTX_WINDOW_BYTES` откалиброван по фактическому размеру транскрипта в момент ротации.

## Е. Пайплайн и эволюция

- [ ] 14 скилов на месте (`harness/skills/`); при доставленном `UNIFIED/` — `СБОРКА-СКИЛОВ.py` идемпотентен (повторный прогон без диффа).
- [ ] Пустой `skills.jsonl` и `gates.jsonl` заведены коллектором; вызов скила / пробный коммит добавляют строки.
- [ ] Учёт токенов: `tokens.jsonl` наполняется после первой сессии; строка суточного расхода видна в утренней сводке.
- [ ] Полуавтомат кодом: выкат в semi без свежего согласия в `confirmations.jsonl` → ворота «занято»; после «подтверждаю» в Telegram — проходит.
- [ ] Ротация full: после принудительной ротации агент ПЕРЕЗАПУЩЕН надзирателем (в панели tmux — claude), сессия жива.
- [ ] `evo-collector.sh` прогнан: сводка создана, журнал-дефектов и реестр-механизмов заведены (пустые).
- [ ] Первая задача продукта прошла весь цикл (03-ЗАДАНИЕ §4); разрывы цикла записаны в память с датой.
- [ ] **: владелец** — отчёт о первой задаче честный: указано, что проверено живым, что «ждёт твоей проверки».

## Ж. Финал

- [ ] Каждый файл каталога `harness/` упомянут в `harness/ОПИСЬ.md` (сверка: `find харнес -type f` против таблиц описи).
- [ ] Приёмка каждой УСТАНОВЛЕННОЙ возможности пройдена по её `ЧЕК-ЛИСТ.md`; реестр действий владельца отправлен ему вложением и получение подтверждено.
- [ ] **: владелец** — решение о переходе semi → full (или остаёмся в semi до N задач).
