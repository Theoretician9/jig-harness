# <Название работы>

Источник: <бриф владельца от ДАТА / макет `путь` / репорт>.
Решение владельца: «<цитата, если есть — цитата дороже пересказа>».

Статус: <итерация N — после ревью / ждёт согласования / СОГЛАСОВАНО>.

## 0. Отдельно и вперёд

<Только если попутно найдено то, что чинится РАНЬШЕ основной работы: обман пользователя,
блокер, утечка. Отдельным коммитом до всего остального. Если нечего — раздел убрать.>

## 1. Зачем

<Проблема словами продукта, не кода. Что сейчас не так для того, кто этим пользуется.>

Ограничение: <что нельзя сломать>.

## 2. Что берём и что не берём

| Из макета / брифа | Решение |
|---|---|
| <элемент> | **Берём** — <почему> |
| <элемент> | Уже есть (`компонент`) — сверить |
| <элемент> | **Не берём** — <причина> |

> «Не берём» **обязательно с причиной**: иначе через месяц это заведут заново.

## 3. <Механика №1>

### 3.1. Состояния

<Перечислить ВСЕ состояния, включая пустое и ошибочное. Пустое состояние — самое забываемое.>

1. **<Состояние>.** <Что видит пользователь. Что делает система.>
2. **<Состояние>.** …

### 3.2. Формат данных

<Точные имена полей, типы, где хранится. Существующие поля называть существующими
именами — не вводить синоним для того, что уже есть.>

### 3.3. Проверенные факты

**Проверено по коду и базе <дата>:**

- <Факт с указанием, чем проверен: запрос, файл, строка.>

> Разделять проверенное и предполагаемое явно. Спека, назвавшая неверную причину,
> заставляет починить не то.

## 4. <Механика №2>

…

## N. Что это меняет в существующем

| Файл / модуль | Что меняется | Риск |
|---|---|---|
| `путь:строка` | <...> | <что сломается, если сделать неаккуратно> |

## N+1. Вне объёма

- <Что осознанно не делаем и почему.>
- <Что уходит в следующую итерацию, под каким id задачи.>

## Приложение А. Ревью спеки

<Заполняется после adversarial-ревью. Каждая находка: severity, суть, решение владельца
или автора. Находка без решения — незакрытый риск, спека не готова.>

| # | Severity | Находка | Решение |
|---|---|---|---|
| C1 | CRITICAL | <...> | <...> |
| I1 | IMPORTANT | <...> | <...> |
| M1 | MINOR | <...> | принято/отложено |
