# Журнал изменений

Все заметные изменения харнеса. Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
номера версий — [SemVer](https://semver.org/lang/ru/): мажор ломает существующую
установку, минор добавляет возможность, патч чинит.

Каждая секция собрана машиной из заголовков коммитов пакета, из которого
собран релиз. Полная история — в этом же репозитории, `git log`.

## [0.1.0] — 2026-09-12

Собрано из пакета `44984f6`. Изменений: 222.

- приёмка: итог не теряется за длинным stderr, таймаут это не провал
- проба входящих: подставной токен помечен как не-секрет
- входящее не сбивает шаг: срочность данными, свод на рубеже, карточка кодом
- ревью релизов: артефакты релиза переживают сборку, теги считаются по опубликованным
- проба второго релиза: журнал накапливает секции
- релизы по стандарту: версия SemVer, тег vX.Y.Z, журнал изменений
- поводы вызова модели — данные: зачем, когда, что делает код
- реестр и CLAUDE.md: механизмов 114, ключ потолка вызовов модели
- стандарт: код спрашивает модель и получает данные по схеме
- Новый скрипт: sprosit-model.py для выбора модели
- прибор механизмов не запрещает самотест публикации
- гейт «функция до определения» допускает заглушку с отступом
- лимит подписки ловится машинным признаком, а молчание смены доходит до владельца
- чистая установка: сухой прогон на голой машине доходит до конца
- панель читает то, что показывает: отчёт расхода 0644 и гейт прав
- раскладка копирует только изменившееся: гейт свежести больше не будят зря
- ревью публикации: одиннадцать важных находок закрыты
- каталог пакета латиницей: STARTER-PACKAGE
- публичная сборка: служебное→sluzhebnoe, разработка→razrabotka (в них лежит код)
- каталоги кода латиницей: harness, panel, vozmozhnosti, gates
- CLAUDE.md пакета собирается из данных, по-английски; раскладка не затирает живые правила
- проба сборщика знает обе раскладки генератора
- раскладка связывает скилы с оболочкой: на чистой установке их не видел никто
- сборка: исходники правил уезжают в служебный подкаталог
- расширения: данные, код, демон, гейт; два скила по MIT
- гейт quotepath: собственные образцы не считаются находкой
- имена: три файла кода латиницей; гейт против слепоты git на кириллице
- ревью кода: четыре критичных находки закрыты
- публикация: удалённый в сборке настраивается всегда, не только при init
- сторож: правило ожидания судит текст без heredoc
- публикация: сборка вместо пакета — замки секретов и чистоты, коммит потомком
- публикация: сборка вместо пакета — замки секретов и чистоты, коммит потомком
- обновление: публичный коммит из своего пакета — не новая версия
- установщик: улика в комментарии без нашего пути
- пакет: последние вшитые пути установки заменены чтением паспорта
- пакет: путь пакета выводится из паспорта; образцы тестов — выдуманная установка
- пакет: наш путь перестал быть умолчанием; юнит панели собирается из паспорта
- гейт(публикация): чистота публичной сборки по образцам из паспорта и git
- сторож: ожидание без предела времени отменяется в момент запуска
- сборка(публикация): сборщик публичного дерева из архива пакета по данным
- …и ещё 182 изменений — см. `git log` пакета
