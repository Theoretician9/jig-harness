# STATE.md — что верно прямо сейчас

> Жанр файла: ТОЛЬКО текущее состояние. Перезаписывается, не дополняется.
> Потолок — 10 000 символов, держится гейтом `scripts/check-context-hygiene.py`.
> История — `docs/handover/`, грабли — `память/`, статусы задач — `dev-map.yaml`.

Обновлено: 08.08.2026 (сборка стартового пакета).

## Где мы

- **Харнес развёрнут, продукт не начат.** Кода продукта нет, репозиторий содержит
  только каркас: правила, память, карту, хуки, демоны, скилы.
- **Ждём первую задачу от владельца** в Telegram. До неё разделы «Домен» и
  «Архитектура» в CLAUDE.md пусты намеренно — заполняются первой задачей.
- Режим — полуавтомат (`AUTONOMY="semi"`): выкат, ротация, необратимое — только
  после анонса и подтверждения владельца.

## Следующий шаг

**Чек-лист приёмки харнеса** (dev-map: `hr.accept`, план работ — 03-ЗАДАНИЕ):
доставка в канал доказана фактом получения; сторож guard_bash отменяет больной
случай; демоны оставляют свежие heartbeat-метки; бэкап снят и восстановим;
числа потолков гигиены сверены с фактическим тарифом VPS.

После приёмки: принудительная первая ротация сессии (`hr.rotation`), затем —
первая задача продукта через весь пайплайн (`pr.first`).

## Заблокировано

- Ничего. Открытые позиции установки (ресурсы сервера, токен бота, имя проекта)
  закрываются при установке — см. 00-ВВОДНЫЕ §5; работу агента они не блокируют.

## Активные грабли

- Пока пусто. Первая же купленная грабля — записью в `память/` + строкой здесь,
  если она действует прямо сейчас.
